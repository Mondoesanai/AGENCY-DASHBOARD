{
  "name": "agency-dashboard",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Agency portfolio dashboard: pulls traffic, SEO, speed and uptime from published client sites, tracks month-over-month growth, and auto-sends a branded monthly report + email on each client's billing date.",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "@vercel/kv": "^3.0.0",
    "resend": "^4.0.0",
    "sharp": "^0.33.5"
  }
}
